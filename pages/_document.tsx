import { Html, Head, Main, NextScript } from 'next/document';

export default function Document() {
	return (
		<Html lang="en">
			<Head></Head>
			<body className="bg-center bg-cover bg-microChip">
				<Main />
				<NextScript />
			</body>
		</Html>
	);
}
